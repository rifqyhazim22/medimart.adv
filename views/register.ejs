<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar - MediMart</title>
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Outfit:wght@300;400;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/login.css">
    <!-- Theme Initialization (Prevent FOUC) -->
    <script>
        const savedTheme = localStorage.getItem('medimart_theme') || 'light';
        document.documentElement.setAttribute('data-theme', savedTheme);
    </script>
    <!-- SweetAlert2 & Animate.css for psychological UI modals -->
    <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/animate.css/4.1.1/animate.min.css" />
    <style>
        .psych-modal {
            border-radius: 20px !important;
        }
    </style>
</head>

<body>
    <!-- Floating Theme Toggle -->
    <button class="theme-toggle-float" id="themeToggleBtn" onclick="toggleTheme()" title="Ganti Siang/Malam">🌙</button>

    <div class="login-container">
        <!-- Left Side: Branding -->
        <div class="login-left">
            <div class="brand-content">
                <div class="logo">
                    <div class="logo-icon">📝</div>
                    <h1>MediMart</h1>
                </div>
                <p class="tagline">Bergabunglah dengan ribuan pengguna lain<br>untuk hidup yang lebih sehat.</p>

                <div class="features">
                    <div class="feature-item">
                        <span class="feature-icon">🚀</span>
                        <div>
                            <h3>Daftar Cepat</h3>
                            <p>Proses pendaftaran mudah, hanya butuh waktu kurang dari 1 menit.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">🏷️</span>
                        <div>
                            <h3>Promo Eksklusif</h3>
                            <p>Dapatkan akses ke promo dan diskon khusus member baru.</p>
                        </div>
                    </div>
                    <div class="feature-item">
                        <span class="feature-icon">💳</span>
                        <div>
                            <h3>Transaksi Aman</h3>
                            <p>Sistem pembayaran terintegrasi dan aman untuk setiap transaksi.</p>
                        </div>
                    </div>
                </div>
            </div>
        </div>

        <!-- Right Side: Form -->
        <div class="login-right">
            <div class="login-box">
                <div class="login-header">
                    <h2>Buat Akun Baru</h2>
                    <p>Lengkapi data diri Anda untuk mendaftar</p>
                </div>

                <% if (error && error.length> 0) { %>
                    <div class="alert alert-error"
                        style="background-color: #fee2e2; color: #b91c1c; padding: 12px; border-radius: 8px; margin-bottom: 20px;">
                        <%= error %>
                    </div>
                    <% } %>

                        <form id="registerForm" action="/auth/register" method="POST" class="login-form">
                            <div class="form-group" style="margin-bottom: 24px;">
                                <label style="margin-bottom: 8px; display: block; font-weight: 600;">Saya ingin
                                    mendaftar sebagai:</label>
                                <div style="display: flex; gap: 12px; margin-bottom: 16px;">
                                    <label class="role-option"
                                        style="flex: 1; cursor: pointer; border: 2px solid var(--border); padding: 12px; border-radius: 10px; text-align: center; font-weight: 500; transition: all 0.2s; position: relative;"
                                        onclick="selectRegRole(this)">
                                        <input type="radio" name="role" value="customer" checked
                                            style="margin-right: 6px;">
                                        Pelanggan
                                    </label>
                                    <label class="role-option"
                                        style="flex: 1; cursor: pointer; border: 2px solid var(--border); padding: 12px; border-radius: 10px; text-align: center; font-weight: 500; transition: all 0.2s;"
                                        onclick="selectRegRole(this)">
                                        <input type="radio" name="role" value="seller" style="margin-right: 6px;">
                                        Seller
                                    </label>
                                </div>
                            </div>

                            <div class="form-group">
                                <label>Nama Lengkap / Nama Toko</label>
                                <div class="input-wrapper">
                                    <span class="input-icon">📝</span>
                                    <input type="text" name="full_name" required placeholder="Nama Lengkap Anda">
                                </div>
                            </div>

                            <div class="form-group">
                                <label>Username</label>
                                <div class="input-wrapper">
                                    <span class="input-icon">@</span>
                                    <input type="text" name="username" required placeholder="Buat username unik">
                                </div>
                            </div>

                            <div class="form-group">
                                <label>Email</label>
                                <div class="input-wrapper">
                                    <span class="input-icon">📧</span>
                                    <input type="email" name="email" required placeholder="nama@email.com">
                                </div>
                            </div>

                            <div class="form-group">
                                <label>Password</label>
                                <div class="input-wrapper">
                                    <span class="input-icon">🔒</span>
                                    <input type="password" name="password" required placeholder="Minimal 6 karakter">
                                    <button type="button" class="toggle-password" onclick="togglePassword()">
                                        <span id="toggleIcon">👁️</span>
                                    </button>
                                </div>
                            </div>

                            <button type="submit" class="btn-login" style="margin-top: 20px;">
                                <span>Daftar Sekarang</span>
                                <span class="btn-icon">✨</span>
                            </button>
                        </form>

                        <div class="login-footer">
                            <p>Sudah punya akun? <a href="/login" class="link-register">Login disini</a></p>
                            <br>
                            <a href="/" class="link-register"
                                style="color: var(--text-gray); font-weight: normal; font-size: 14px;">← Kembali ke
                                Beranda</a>
                        </div>
            </div>
        </div>
    </div>

    <script>
        function togglePassword() {
            const passwordInput = document.querySelector('input[name="password"]');
            const toggleIcon = document.getElementById('toggleIcon');
            if (passwordInput.type === 'password') {
                passwordInput.type = 'text';
                toggleIcon.textContent = '🙈';
            } else {
                passwordInput.type = 'password';
                toggleIcon.textContent = '👁️';
            }
        }

        // Initialize styling for checked radio
        function updateRoleStyles() {
            document.querySelectorAll('.role-option').forEach(label => {
                const radio = label.querySelector('input');
                if (radio.checked) {
                    label.style.borderColor = '#FF6B35';
                    label.style.backgroundColor = 'rgba(255, 107, 53, 0.08)';
                    label.style.color = '#FF6B35';
                } else {
                    label.style.borderColor = 'var(--border)';
                    label.style.backgroundColor = 'transparent';
                    label.style.color = 'var(--text-dark)';
                }
            });
        }

        function selectRegRole(label) {
            // Radio button click propagates, so we just update styles
            // Use setTimeout to allow the event to process the check state
            setTimeout(updateRoleStyles, 0);
        }

        // Run on load
        updateRoleStyles();

        // Theme Toggle Function
        function toggleTheme() {
            const current = document.documentElement.getAttribute('data-theme');
            const target = current === 'dark' ? 'light' : 'dark';
            document.documentElement.setAttribute('data-theme', target);
            localStorage.setItem('medimart_theme', target);
            document.getElementById('themeToggleBtn').innerHTML = target === 'dark' ? '☀️' : '🌙';
        }

        // Set correct toggle icon on load
        document.addEventListener('DOMContentLoaded', () => {
            const current = document.documentElement.getAttribute('data-theme');
            const btn = document.getElementById('themeToggleBtn');
            if (btn) btn.innerHTML = current === 'dark' ? '☀️' : '🌙';
        });

        // 🧠 Intersepsi Pendaftaran Eksklusif (Efek Kehebatan IT)
        const regForm = document.getElementById('registerForm');
        if (regForm) {
            regForm.addEventListener('submit', function (e) {
                e.preventDefault();
                Swal.fire({
                    title: 'Menciptakan Akun Anda..',
                    html: '<span style="color: var(--text-gray); font-size:15px;">Mengenkripsi kata sandi dan mendaftarkan entitas Anda 🛡️</span>',
                    allowOutsideClick: false,
                    showConfirmButton: false,
                    willOpen: () => Swal.showLoading(),
                    showClass: { popup: 'animate__animated animate__zoomIn animate__faster' },
                    customClass: { popup: 'psych-modal' }
                });

                setTimeout(() => this.submit(), 1500);
            });
        }
    </script>
</body>

</html>