<!DOCTYPE html>
<html lang="id">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Lupa Password - MediMart</title>
    <link
        href="https://fonts.googleapis.com/css2?family=DM+Sans:wght@400;500;700&family=Outfit:wght@300;400;600;700;800&display=swap"
        rel="stylesheet">
    <link rel="stylesheet" href="/css/login.css">
</head>

<body>
    <div class="login-container">
        <div class="login-left">
            <div class="brand-content">
                <div class="logo">
                    <div class="logo-icon">🔒</div>
                    <h1>MediMart</h1>
                </div>
                <p class="tagline">Kembalikan akses akun Anda<br>dengan mudah dan aman.</p>
            </div>
        </div>

        <div class="login-right">
            <div class="login-box">
                <div class="login-header">
                    <h2>Lupa Password?</h2>
                    <p>Masukkan email Anda untuk reset password.</p>
                </div>

                <% if (error && error.length> 0) { %>
                    <div class="alert alert-error"
                        style="background-color: #fee2e2; color: #b91c1c; padding: 12px; border-radius: 8px; margin-bottom: 20px;">
                        <%= error %>
                    </div>
                    <% } %>

                        <% if (success_msg && success_msg.length> 0) { %>
                            <div class="alert alert-success"
                                style="background-color: #d1fae5; color: #065f46; padding: 12px; border-radius: 8px; margin-bottom: 20px;">
                                <%= success_msg %>
                            </div>
                            <% } %>

                                <form action="/auth/forgot-password" method="POST" class="login-form">
                                    <div class="form-group">
                                        <label>Email Terdaftar</label>
                                        <div class="input-wrapper">
                                            <span class="input-icon">📧</span>
                                            <input type="email" name="email" required placeholder="nama@email.com">
                                        </div>
                                    </div>

                                    <button type="submit" class="btn-login" style="margin-top: 20px;">
                                        <span>Kirim Link Reset</span>
                                        <span class="btn-icon">🚀</span>
                                    </button>
                                </form>

                                <div class="login-footer">
                                    <a href="/login" class="link-register"
                                        style="color: var(--text-gray); font-weight: normal; font-size: 14px;">← Kembali ke
                                        Login</a>
                                </div>
            </div>
        </div>
    </div>
</body>

</html>